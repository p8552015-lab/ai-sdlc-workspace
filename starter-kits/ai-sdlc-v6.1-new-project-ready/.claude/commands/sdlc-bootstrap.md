# /sdlc-bootstrap

Use after architecture approval for a new project.

Claude Code may create framework scaffold, lint, formatter, tests, CI, README, `.env.example`, health check/smoke test, and basic folder structure.

Claude Code must not implement broad product features or use production secrets.

End with exact commands run and verification evidence.
